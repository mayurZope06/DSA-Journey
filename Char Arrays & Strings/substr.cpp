#include <iostream>
using namespace std;
int main()
{
    string name = "Hello jee kaise ho saare";
    cout<<name.substr(0,5)<<endl;
    cout<<name.substr(0)<<endl;
    cout<<name.substr(5);

    return 0;
}